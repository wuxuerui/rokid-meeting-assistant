export const pageTitle = 'ESM Import';
export const moduleName = 'constants.js';
export const directBadge = 'named export ready';
export const directDetails = {
  kind: 'page -> js',
  owner: 'pages/esm/index.ink',
  exports: 'default + named',
};

const directModule = {
  description: 'Direct ESM module imported from the page entry.',
  sampleCount: 4,
  emphasis: 'success-only fixture',
};

export default directModule;
